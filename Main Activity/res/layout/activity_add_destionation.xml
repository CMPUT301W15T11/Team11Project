<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AddDestionation" >

    <Button
        android:id="@+id/addDest"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:text="Add" />

    <EditText
        android:id="@+id/newDestTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/addDest"
        android:layout_alignBottom="@+id/addDest"
        android:layout_alignParentLeft="true"
        android:ems="10"
        android:hint="New Destination" >

        <requestFocus />
    </EditText>

    <ListView
        android:id="@+id/existingDestsListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/newDestTextView"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/addDest" >

    </ListView>

</RelativeLayout>
